<%= form_tag wikis_add_collaborators_path do %>
  <%= label_tag :add_collaborator %>
  <%= hidden_field_tag :id, @wiki.id %>
  <%= select_tag :user_id , options_from_collection_for_select(User.all, 'id', 'email') %>
  <%= submit_tag "Save", class: 'btn btn-success' %>
<% end %>

<%= form_tag wikis_remove_collaborators_path do %>
  <%= label_tag :remove_collaborator %>
  <%= hidden_field_tag :id, @wiki.id %>
  <%= select_tag :user_id , options_from_collection_for_select(@wiki.users, 'id', 'email') %>
  <%= submit_tag "Delete", class: 'btn btn-danger' %>
<% end %>